name: Publish to NPM

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (skip actual publish)'
        required: false
        default: false
        type: boolean

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Scope packages for @${{ github.repository_owner }}
        run: bun scripts/scope-packages.ts ${{ github.repository_owner }}

      - name: Build only publishable packages
        run: |
          # Build cross-path first (no dependencies)
          (cd packages/cross-path && bun run build)
          # Build swarm-mail (dependency)
          (cd packages/swarm-mail && bun run build)
          # Build opencode-swarm-plugin
          (cd packages/opencode-swarm-plugin && bun run build)

      - name: Publish to NPM
        if: ${{ inputs.dry_run != true }}
        run: |
          set -euo pipefail

          for pkg in packages/cross-path packages/swarm-mail packages/opencode-swarm-plugin; do
            echo "📦 Publishing $pkg..."
            # Use bun publish to properly resolve workspace:* dependencies
            # Always publish as beta - this is a fork, not the canonical release
            (cd "$pkg" && bun publish --access public --tag beta) || echo "⚠️ May already exist"
          done
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Dry run - show what would be published
        if: ${{ inputs.dry_run == true }}
        run: |
          for pkg in packages/cross-path packages/swarm-mail packages/opencode-swarm-plugin; do
            echo "📦 Would publish $pkg:"
            cat "$pkg/package.json" | jq '{name, version}'
          done
