name: Publish to NPM

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (skip actual publish)'
        required: false
        default: false
        type: boolean

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Scope packages for @${{ github.repository_owner }}
        run: bun scripts/scope-packages.ts ${{ github.repository_owner }}

      - name: Refresh workspace after scoping
        run: |
          # Delete old lockfile - it references unscoped package names
          rm bun.lock
          # Regenerate lockfile with the new scoped package names
          bun install

      - name: Build all packages
        run: |
          # Build in dependency order:
          # 1. swarm-cross-path (no workspace deps)
          (cd packages/cross-path && bun run build)
          # 2. swarm-mail (depends on swarm-cross-path)
          (cd packages/swarm-mail && bun run build)
          # 3. swarm (depends on swarm-cross-path & swarm-mail)
          (cd packages/opencode-swarm-plugin && bun run build)
          # 4. swarm-evals (depends on swarm & swarm-mail)
          (cd packages/swarm-evals && bun run build || true)
          # 5. swarm-claude-code (no workspace deps)
          (cd packages/claude-code-swarm-plugin && bun run build || true)
          # 6. swarm-dashboard (depends on swarm-mail)
          (cd packages/swarm-dashboard && bun run build || true)

      - name: Publish to NPM
        if: ${{ inputs.dry_run != true }}
        run: |
          set -euo pipefail

          # Publish ALL 6 packages in dependency order:
          # 1. swarm-cross-path (no deps)
          # 2. swarm-mail (depends on swarm-cross-path)
          # 3. swarm (depends on swarm-cross-path & swarm-mail)
          # 4. swarm-evals (depends on swarm & swarm-mail)
          # 5. swarm-claude-code (no workspace deps)
          # 6. swarm-dashboard (depends on swarm-mail)
          PACKAGES=(
            packages/cross-path
            packages/swarm-mail
            packages/opencode-swarm-plugin
            packages/swarm-evals
            packages/claude-code-swarm-plugin
            packages/swarm-dashboard
          )

          for pkg in "${PACKAGES[@]}"; do
            echo "📦 Publishing $pkg..."
            # bun publish resolves workspace:* to actual versions automatically
            # --access public required for scoped packages
            # --tag beta marks these as beta releases
            (cd "$pkg" && bun publish --access public --tag beta) || echo "⚠️ May already exist"
          done
        env:
          # bun publish uses NPM_CONFIG_TOKEN (not NODE_AUTH_TOKEN)
          NPM_CONFIG_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Dry run - show what would be published
        if: ${{ inputs.dry_run == true }}
        run: |
          PACKAGES=(
            packages/cross-path
            packages/swarm-mail
            packages/opencode-swarm-plugin
            packages/swarm-evals
            packages/claude-code-swarm-plugin
            packages/swarm-dashboard
          )
          for pkg in "${PACKAGES[@]}"; do
            echo "📦 Would publish $pkg:"
            cat "$pkg/package.json" | jq '{name, version, dependencies}'
          done
